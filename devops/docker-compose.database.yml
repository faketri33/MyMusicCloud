services:
  auth_base:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      GDATA: /data/postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres
    networks:
      - app-tier